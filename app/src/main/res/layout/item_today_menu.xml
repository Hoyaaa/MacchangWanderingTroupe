<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="8dp"
    android:paddingBottom="16dp">

    <!-- 카드: 정사각 비율 유지 -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_food"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:cardCornerRadius="12dp"
        app:cardUseCompatPadding="true"
        app:strokeColor="#DDDDDD"
        app:strokeWidth="1dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="1:1">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/iv_food"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:contentDescription="음식 이미지"
                android:background="#D9D9D9"/>

            <!-- 플레이스홀더 텍스트(이미지 없을 때만 보이도록 코드에서 제어) -->
            <TextView
                android:id="@+id/tv_placeholder"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="음식 이미지"
                android:textStyle="bold"
                android:textSize="18sp"
                android:textColor="#222222"
                android:visibility="gone"
                android:layout_gravity="center"/>
        </FrameLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- 음식 이름(#태그 느낌) : 카드 폭 기준 좌측 정렬 -->
    <TextView
        android:id="@+id/tv_food_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="# 음식이름"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#222222"
        android:gravity="start"
        app:layout_constraintTop_toBottomOf="@id/card_food"
        app:layout_constraintStart_toStartOf="@id/card_food"
        app:layout_constraintEnd_toEndOf="@id/card_food"/>

    <!-- 즐겨찾기: 이름 '바로 아래' + 카드 기준 '가로 중앙' -->
    <ImageButton
        android:id="@+id/btn_fav"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginTop="8dp"
        android:background="@null"
        app:srcCompat="@drawable/ic_star_border_24"
        android:scaleType="centerInside"
        app:layout_constraintTop_toBottomOf="@id/tv_food_name"
        app:layout_constraintStart_toStartOf="@id/card_food"
        app:layout_constraintEnd_toEndOf="@id/card_food"/>

</androidx.constraintlayout.widget.ConstraintLayout>
